package com.upthink.Canavas_QMS.repository;import com.upthink.Canavas_QMS.model.EssayDetails;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.data.jpa.repository.Modifying;import org.springframework.data.jpa.repository.Query;import org.springframework.stereotype.Repository;import org.springframework.transaction.annotation.Transactional;import java.sql.Timestamp;import java.util.List;import java.util.Optional;@Repositorypublic interface EssayDetailsRepository extends JpaRepository<EssayDetails, Integer> {    // Custom SQL Queries (excluding soft-deleted records)    @Query("SELECT e FROM EssayDetails e WHERE e.deletedAt IS NULL")    List<EssayDetails> findAllEssayDetails();    @Query("SELECT e FROM EssayDetails e WHERE e.id = :id AND e.deletedAt IS NULL")    Optional<EssayDetails> findEssayDetailsById(Integer id);    @Modifying    @Transactional    @Query("UPDATE EssayDetails e SET e.deletedAt = :deletedAt WHERE e.id = :id")    void softDeleteEssayDetailsById(Integer id, Timestamp deletedAt);}