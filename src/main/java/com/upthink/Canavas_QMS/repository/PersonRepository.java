package com.upthink.Canavas_QMS.repository;import com.upthink.Canavas_QMS.model.Person;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.data.jpa.repository.Modifying;import org.springframework.data.jpa.repository.Query;import org.springframework.stereotype.Repository;import org.springframework.transaction.annotation.Transactional;import java.util.List;import java.util.Optional;@Repositorypublic interface PersonRepository extends JpaRepository<Person, String> {    // Custom SQL Queries (excluding inactive persons)    @Query("SELECT p FROM Person p WHERE p.active = true")    List<Person> findAllPersons();    @Query("SELECT p FROM Person p WHERE p.id = :id AND p.active = true")    Optional<Person> findPersonById(String id);    @Modifying    @Transactional    @Query("UPDATE Person p SET p.active = false WHERE p.id = :id")    void softDeletePersonById(String id);}